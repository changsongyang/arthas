import{_ as d,o as l,c,a as e,b as a,w as n,e as t,d as o,r}from"./app.e2427bec.js";const u={},h=e("h1",{id:"tips-for-complex-ognl-expressions",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#tips-for-complex-ognl-expressions","aria-hidden":"true"},"#"),t(" Tips for Complex OGNL Expressions")],-1),p=e("p",null,[t("This page covers complex but production-friendly OGNL patterns in Arthas, including variables, collection selection and projection, temporary result construction, null handling, and ClassLoaders. Every complete command in the \u201CVerified complex expressions\u201D section was run in a real Arthas attach session against the repository's "),e("code",null,"math-game"),t(" JVM. Dynamic values such as thread names, counts, and object addresses will vary by environment.")],-1),m=t("Read the "),v=e("code",null,"ognl",-1),b=t(" command documentation"),g=t(" for basic options and "),x=t("Core variables in expressions"),f=t(" for variables exposed by commands such as "),y=e("code",null,"watch",-1),_=t("."),k=o('<div class="custom-container warning"><p class="custom-container-title">WARNING</p><p>OGNL runs inside the target JVM. A method call can read or modify application state. In production, generate and execute read-only expressions by default, and limit both the number of objects and the expansion depth. Do not invoke application methods such as <code>set*</code>, <code>add</code>, <code>remove</code>, or <code>clear</code>, and do not perform file, network, process, ClassLoader, privilege-escalating reflection, or thread-control operations. Calls to <code>put</code> in this page only modify a temporary <code>Map</code> created by the same expression.</p></div><h2 id="identify-the-expression-context-first" tabindex="-1"><a class="header-anchor" href="#identify-the-expression-context-first" aria-hidden="true">#</a> Identify the expression context first</h2><p>The grammar may be the same while the available variables are different. A common AI agent mistake is to copy context variables from another command into the standalone <code>ognl</code> command.</p>',3),q=e("thead",null,[e("tr",null,[e("th",null,"Location"),e("th",null,"Available root objects or variables"),e("th",null,"Common mistake")])],-1),w=e("tr",null,[e("td",null,[e("code",null,"ognl"),t(" command")]),e("td",null,[t("Static members, constructors, literals, and self-defined "),e("code",null,"#variables")]),e("td",null,[t("Using "),e("code",null,"params"),t(", "),e("code",null,"target"),t(", "),e("code",null,"returnObj"),t(", or "),e("code",null,"instances")])],-1),j=e("tr",null,[e("td",null,[e("code",null,"getstatic"),t(" expression")]),e("td",null,"The static field value being read is the root object"),e("td",null,[t("Treating it as a standalone "),e("code",null,"ognl"),t(" expression with no root object")])],-1),L=e("td",null,[t("Observation or condition expressions in "),e("code",null,"watch"),t(", "),e("code",null,"stack"),t(", "),e("code",null,"monitor"),t(", "),e("code",null,"tt"),t(", and related commands")],-1),A=e("code",null,"Advice",-1),S=t(" variables such as "),N=e("code",null,"params",-1),C=t(", "),T=e("code",null,"target",-1),O=t(", "),M=e("code",null,"returnObj",-1),E=t(", "),I=e("code",null,"throwExp",-1),D=t(", and "),G=e("code",null,"isReturn",-1),R=e("td",null,[t("Reading a return value at method entry, or invoking "),e("code",null,"returnObj"),t(" on the exception path")],-1),V=e("tr",null,[e("td",null,[t("Conditions in "),e("code",null,"trace"),t(" and related commands")]),e("td",null,[t("Advice variables exposed by that command; some commands also provide "),e("code",null,"#cost")]),e("td",null,[t("Assuming that every command provides "),e("code",null,"#cost")])],-1),J=e("tr",null,[e("td",null,[e("code",null,"line --express"),t(" / "),e("code",null,"--condition")]),e("td",null,[t("Advice variables plus line-level variables such as "),e("code",null,"lineNumber"),t(", "),e("code",null,"localVarMap"),t(", and "),e("code",null,"#cost")]),e("td",null,[t("Using "),e("code",null,"localVarMap"),t(" or "),e("code",null,"lineNumber"),t(" in another command")])],-1),P=e("tr",null,[e("td",null,[e("code",null,"vmtool --express")]),e("td",null,[e("code",null,"instances"),t(", the array returned by "),e("code",null,"getInstances")]),e("td",null,[t("Using "),e("code",null,"instances"),t(" in the standalone "),e("code",null,"ognl"),t(" command")])],-1),F=o(`<p>Also distinguish a complete Arthas command from a raw expression parameter:</p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code><span class="token comment"># The Arthas console needs the full command and outer single quotes</span>
ognl <span class="token string">&#39;#value=@java.lang.System@getProperty(&quot;java.version&quot;), #value&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>When an MCP tool, HTTP API, or another client already provides a dedicated parameter named <code>expression</code>, <code>express</code>, or <code>conditionExpress</code>, pass only the raw expression. Do not add <code>ognl</code>, command options, outer single quotes, or a Markdown code fence:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>#value=@java.lang.System@getProperty(&quot;java.version&quot;), #value
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="reliable-syntax-building-blocks" tabindex="-1"><a class="header-anchor" href="#reliable-syntax-building-blocks" aria-hidden="true">#</a> Reliable syntax building blocks</h2><table><thead><tr><th>Purpose</th><th>Syntax</th><th>Notes</th></tr></thead><tbody><tr><td>Define a temporary variable</td><td><code>#name=value</code></td><td>A custom variable must start with <code>#</code></td></tr><tr><td>Evaluate in sequence and return the last item</td><td><code>#a=1, #b=2, #a+#b</code></td><td>Use commas, not semicolons; Arthas may treat a semicolon as a command separator</td></tr><tr><td>Access a static method or field</td><td><code>@fully.qualified.Class@method()</code>, <code>@fully.qualified.Class@FIELD</code></td><td>Both <code>@</code> characters are required; prefer the fully qualified class name</td></tr><tr><td>Create a temporary object</td><td><code>new java.util.LinkedHashMap()</code></td><td>Do not include Java generics such as <code>new HashMap&lt;String,Object&gt;()</code></td></tr><tr><td>Create a List</td><td><code>{1, 2, 3}</code></td><td>The result is a List, not a Java array</td></tr><tr><td>Create a Map</td><td><code>#{&quot;name&quot;:&quot;arthas&quot;, &quot;count&quot;:1}</code></td><td>Do not insert anything between <code>#</code> and <code>{</code></td></tr><tr><td>Create an array</td><td><code>new java.lang.String[]{&quot;a&quot;, &quot;b&quot;}</code></td><td>The array type must be explicit</td></tr><tr><td>Project a collection</td><td><code>items.{#this.getName()}</code></td><td>Evaluates each item and returns a new List</td></tr><tr><td>Select from a collection</td><td><code>items.{? #this != null}</code></td><td>Returns every matching item</td></tr><tr><td>Select the first or last match</td><td><code>items.{^ condition}</code>, <code>items.{$ condition}</code></td><td>The result is still a List; account for an empty result before reading <code>[0]</code></td></tr><tr><td>Refer to the current item</td><td><code>#this</code></td><td>Its meaning changes with the current level in nested selections and projections</td></tr></tbody></table><p>For complex expressions, explicitly invoke public getters to reduce property-resolution and overload ambiguity. Prefer <code>#this.getName()</code> to <code>#this.name</code>. For maps, prefer <code>get(&quot;key&quot;)</code> and <code>containsKey(&quot;key&quot;)</code> to dotted property shorthand.</p><h2 id="verified-complex-expressions" tabindex="-1"><a class="header-anchor" href="#verified-complex-expressions" aria-hidden="true">#</a> Verified complex expressions</h2><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>Some examples in this section use <code>Thread.getAllStackTraces()</code> to obtain a collection in any demo JVM. This operation captures every thread stack. On a production JVM with many threads, prefer a dedicated command such as <code>thread</code>, or assess the cost first, and do not run these examples frequently.</p></div><h3 id="selection-projection-first-match-and-last-match" tabindex="-1"><a class="header-anchor" href="#selection-projection-first-match-and-last-match" aria-hidden="true">#</a> Selection, projection, first match, and last match</h3><p>The following expression defines a list and returns projection, selection, first-match, and last-match results together:</p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code>ognl <span class="token parameter variable">-x</span> <span class="token number">3</span> <span class="token string">&#39;#numbers={1,2,3,4,5,6}, #{&quot;projection&quot;:#numbers.{#this * 10},&quot;selection&quot;:#numbers.{? #this % 2 == 0},&quot;first&quot;:#numbers.{^ #this % 2 == 0}[0],&quot;last&quot;:#numbers.{$ #this % 2 == 0}[0]}&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Even numbers are known to exist in this literal, so <code>[0]</code> is safe here. For runtime collections, save the selected list and check whether it is empty:</p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code>ognl <span class="token string">&#39;#matched={1,3,5}.{^ #this % 2 == 0}, #matched.isEmpty() ? null : #matched[0]&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="construct-a-structured-result-with-temporary-variables" tabindex="-1"><a class="header-anchor" href="#construct-a-structured-result-with-temporary-variables" aria-hidden="true">#</a> Construct a structured result with temporary variables</h3><p>Do not repeat expensive calls in a long expression. Save intermediate values, then return a newly created <code>LinkedHashMap</code>:</p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code>ognl <span class="token parameter variable">-x</span> <span class="token number">2</span> <span class="token string">&#39;#result=new java.util.LinkedHashMap(), #props=@java.lang.System@getProperties(), #threads=@java.lang.Thread@getAllStackTraces(), #result.put(&quot;javaVersion&quot;, #props.get(&quot;java.version&quot;)), #result.put(&quot;javaHome&quot;, #props.get(&quot;java.home&quot;)), #result.put(&quot;threadCount&quot;, #threads.size()), #result&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>The returned keys are <code>javaVersion</code>, <code>javaHome</code>, and <code>threadCount</code>. Comma-separated subexpressions are evaluated in order, and the value of the whole expression is the final <code>#result</code>.</p><h3 id="select-first-then-return-only-the-required-fields" tabindex="-1"><a class="header-anchor" href="#select-first-then-return-only-the-required-fields" aria-hidden="true">#</a> Select first, then return only the required fields</h3><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code>ognl <span class="token string">&#39;@java.lang.Thread@getAllStackTraces().keySet().{? #this.isAlive() &amp;&amp; !#this.isDaemon()}.{#this.getName()}&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><code>.{? ...}</code> returns the selected threads. The following <code>.{...}</code> projects only thread names, avoiding expansion of complete <code>Thread</code> objects.</p><h3 id="count-by-group" tabindex="-1"><a class="header-anchor" href="#count-by-group" aria-hidden="true">#</a> Count by group</h3><p>The following expression counts threads by state. <code>#counts</code> is a temporary map created by this expression and does not modify an existing application object:</p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code>ognl <span class="token parameter variable">-x</span> <span class="token number">2</span> <span class="token string">&#39;#counts=new java.util.LinkedHashMap(), @java.lang.Thread@getAllStackTraces().keySet().{#state=#this.getState().toString(), #counts.put(#state, #counts.containsKey(#state) ? #counts.get(#state) + 1 : 1)}, #counts&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Example output:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>@LinkedHashMap[
    @String[RUNNABLE]:@Integer[10],
    @String[TIMED_WAITING]:@Integer[6],
    @String[WAITING]:@Integer[2],
]
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Do not put Java Stream syntax such as <code>stream().filter(x -&gt; ...)</code>, method references such as <code>Type::method</code>, or <code>computeIfAbsent(..., x -&gt; ...)</code> into OGNL. OGNL does not parse Java lambdas. Use OGNL selection <code>.{? ...}</code> and projection <code>.{...}</code> instead.</p><h3 id="bind-this-early-in-nested-objects" tabindex="-1"><a class="header-anchor" href="#bind-this-early-in-nested-objects" aria-hidden="true">#</a> Bind <code>#this</code> early in nested objects</h3><p>Inside the projection over <code>entrySet()</code>, <code>#this</code> is the current <code>Map.Entry</code>. Bind its key and value to meaningful variables before using them in a longer expression:</p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code>ognl <span class="token string">&#39;#all=@java.lang.Thread@getAllStackTraces(), #all.entrySet().{? #this.getKey().getName().startsWith(&quot;arthas&quot;)}.{#thread=#this.getKey(), #stack=#this.getValue(), #thread.getName() + &quot; | &quot; + #thread.getState() + &quot; | depth=&quot; + #stack.length}&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="handle-null-explicitly" tabindex="-1"><a class="header-anchor" href="#handle-null-explicitly" aria-hidden="true">#</a> Handle null explicitly</h3><p>OGNL does not support JavaScript-style <code>?.</code>. Do not mix in SpEL <code>T(...)</code> or template syntax <code>\${...}</code> either. Save a possibly null value and protect the method call with a conditional expression:</p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code>ognl <span class="token string">&#39;#value=@java.lang.System@getProperty(&quot;arthas.not.exists&quot;), #value == null ? &quot;&lt;missing&gt;&quot; : #value.trim()&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Result:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>@String[&lt;missing&gt;]
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="handle-both-return-and-exception-paths-in-watch" tabindex="-1"><a class="header-anchor" href="#handle-both-return-and-exception-paths-in-watch" aria-hidden="true">#</a> Handle both return and exception paths in <code>watch</code></h3><p><code>-f</code> evaluates the expression after both a normal return and an exception. Branch on <code>isReturn</code> before accessing <code>returnObj</code> or <code>throwExp</code>:</p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code><span class="token function">watch</span> demo.MathGame primeFactors <span class="token string">&#39;#result=new java.util.LinkedHashMap(), #result.put(&quot;number&quot;, params[0]), #result.put(&quot;outcome&quot;, isReturn ? &quot;return&quot; : &quot;throw&quot;), #result.put(&quot;detail&quot;, isReturn ? returnObj.size() : throwExp.getMessage()), #result&#39;</span> <span class="token parameter variable">-f</span> <span class="token parameter variable">-n</span> <span class="token number">2</span> <span class="token parameter variable">-x</span> <span class="token number">2</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This expression evaluates on both paths in the real <code>math-game</code> JVM. <code>-n 2</code> limits the number of observations so the command terminates.</p><h3 id="use-instances-in-vmtool" tabindex="-1"><a class="header-anchor" href="#use-instances-in-vmtool" aria-hidden="true">#</a> Use <code>instances</code> in <code>vmtool</code></h3><p><code>instances</code> exists only in the expression context of <code>vmtool --action getInstances --express</code>:</p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code>vmtool <span class="token parameter variable">--action</span> getInstances <span class="token parameter variable">--className</span> java.lang.Thread <span class="token parameter variable">--limit</span> <span class="token number">100</span> <span class="token parameter variable">--express</span> <span class="token string">&#39;instances.{? #this.isAlive() &amp;&amp; #this.getName().startsWith(&quot;arthas&quot;)}.{#this.getName()}&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Use <code>--limit</code> before selection and projection. Do not use <code>--limit -1</code> unconditionally for an application class that may have many instances.</p><h2 id="classloader-selection-is-not-ognl-syntax" tabindex="-1"><a class="header-anchor" href="#classloader-selection-is-not-ognl-syntax" aria-hidden="true">#</a> ClassLoader selection is not OGNL syntax</h2><p>Before accessing an application class, identify its ClassLoader:</p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code>sc <span class="token parameter variable">-d</span> com.example.OrderService
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Read <code>classLoaderHash</code> from the output and pass it as an <code>ognl</code> command option:</p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code>ognl <span class="token parameter variable">-c</span> <span class="token operator">&lt;</span>classLoaderHash<span class="token operator">&gt;</span> <span class="token string">&#39;@com.example.OrderService@STATIC_FIELD&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Keep these points in mind:</p><ul><li><code>-c</code> and <code>--classLoaderClass</code> are Arthas command options, not parts of the OGNL expression.</li><li>A class name may be loaded by multiple ClassLoaders. Do not guess the hash, and remember that it may change after a JVM restart.</li><li>For <code>ClassNotFoundException</code>, inspect the ClassLoader before randomly rewriting OGNL syntax.</li><li><code>--classLoaderClass</code> is appropriate only when exactly one ClassLoader instance has that type. Use the hash when multiple instances match.</li></ul><h2 id="quoting-and-formatting-there-are-two-parsers" tabindex="-1"><a class="header-anchor" href="#quoting-and-formatting-there-are-two-parsers" aria-hidden="true">#</a> Quoting and formatting: there are two parsers</h2><p>Arthas parses the command line first, then OGNL parses the expression. A shell, JSON, or MCP transport adds another formatting layer.</p><ul><li>In the Arthas console, wrap a complete expression in single quotes and use double quotes for OGNL string literals.</li><li>Keep the final command on one physical line. Visual terminal wrapping is harmless, but do not insert a real line break inside a class name, <code>.</code>, <code>@</code>, <code>#variable</code>, or an operator.</li><li>Do not insert whitespace after <code>.</code>, as in the invalid form <code>#map. get(&quot;key&quot;)</code>.</li><li>Join expression steps with commas, not semicolons.</li><li>Keep outer single quotes around expressions containing <code>{$ ...}</code>, otherwise some shells will interpret <code>$</code>.</li><li>For an MCP expression parameter, do not include Markdown backticks, outer single quotes, or the complete command.</li><li>For JSON transport, escape double quotes according to JSON rules only. Do not copy shell backslash escaping verbatim into a raw expression parameter.</li></ul>`,53),H=t("When quoting becomes deeply nested, prefer an Arthas "),U=t("batch file"),B=t(" or a dedicated expression field in the API to reduce shell-escaping layers."),W=o(`<h2 id="do-not-classify-every-failure-as-a-syntax-error" tabindex="-1"><a class="header-anchor" href="#do-not-classify-every-failure-as-a-syntax-error" aria-hidden="true">#</a> Do not classify every failure as a syntax error</h2><table><thead><tr><th>Symptom or exception keyword</th><th>Check first</th><th>Recommended action</th></tr></thead><tbody><tr><td><code>Malformed OGNL expression</code>, <code>ExpressionSyntaxException</code></td><td>Quotes, brackets, <code>@class@member</code>, commas, and projection/selection braces</td><td>Reduce to a minimal expression, then add one fragment at a time</td></tr><tr><td><code>NoSuchPropertyException</code></td><td>Whether the command exposes that variable; whether the property name is correct</td><td>Check the context-variable table and use an explicit public getter</td></tr><tr><td><code>MethodFailedException</code>, <code>NoSuchMethodException</code></td><td>Method name, argument types, and static versus instance invocation</td><td>Use <code>sm</code>/<code>jad</code> to inspect the real signature instead of guessing an overload</td></tr><tr><td><code>ClassNotFoundException</code></td><td>ClassLoader</td><td>Run <code>sc -d</code>, then specify <code>-c</code></td></tr><tr><td>A null intermediate object</td><td>Missing null branch in a call chain</td><td>Assign it to a <code>#variable</code> and guard the next call with a conditional expression</td></tr><tr><td><code>stricter invocation mode</code></td><td>The method or declaring class is blocked by OGNL strict mode</td><td>Do not disable the protection or bypass it with reflection; use <code>jvm</code>, <code>thread</code>, <code>sysprop</code>, or another dedicated command, or a safe public API</td></tr><tr><td><code>module ... does not export</code></td><td>JDK module boundaries, not necessarily OGNL grammar</td><td>Use a dedicated Arthas command or an accessible public application API</td></tr><tr><td>Timeout, huge output, or target-JVM disturbance</td><td>Expression work and expansion scope</td><td>Select before projecting, avoid repeated calls, and set <code>--limit</code>, <code>-n</code>, and a reasonable <code>-x</code></td></tr></tbody></table><p>For example, <code>@java.lang.Runtime@getRuntime()</code> can be rejected by the default OGNL strict invocation mode even though its syntax is valid. Repeatedly changing <code>@</code>, brackets, or quotes cannot fix this category of failure.</p><h2 id="generation-and-validation-protocol-for-ai-agents" tabindex="-1"><a class="header-anchor" href="#generation-and-validation-protocol-for-ai-agents" aria-hidden="true">#</a> Generation and validation protocol for AI agents</h2><p>Consider adding the following constraints to prompts for agents that generate Arthas commands:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>You are generating OGNL for Arthas. Before generating it, you must confirm:
1. Whether the caller needs a complete Arthas command or a raw expression parameter. Output only the requested form.
2. Whether the expression runs in the ognl, getstatic, watch/stack/monitor/tt/trace, line, or vmtool context. Use only variables that actually exist in that context.
3. For application classes, run sc -d to identify the ClassLoader. If a method or field is uncertain, inspect it with sm/jad instead of guessing its name or overload.
4. Use @fully.qualified.Class@member for static access, #name for temporary variables, and commas for sequential steps.
5. Do not generate Java lambdas/Streams, method references, ?., SpEL T(...), \${...}, generic constructors, or semicolon statements.
6. Default to read-only behavior. Forbid process, file, network, ClassLoader, privilege-escalating reflection, thread-control, and application-state mutations. Mutation is allowed only on temporary collections created with new inside the expression.
7. Select before projecting, return only required fields, and set reasonable limit, -n, and -x values.
8. Keep the final expression on one physical line. Never break or insert whitespace inside a class name, dot access, @...@, #variable, or operator.

Validate incrementally instead of submitting one untested long expression:
A. Verify that the minimal root object, static field, or context variable is accessible.
B. Add only one #variable assignment, method call, selection, or projection per attempt, and execute it.
C. Add Map/List output construction and limiting options last.
D. On failure, classify it as command/escaping, grammar, context variable, ClassLoader, null, method signature, access restriction, or resource cost. Preserve the validated prefix and change only the failing fragment.
E. Claim that an expression is verified only after receiving a real Arthas result without a syntax exception.
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Before returning output, the agent should also perform a mechanical check:</p><ul><li>Does every <code>@class@member</code> contain a matching pair of <code>@</code> characters?</li><li>Are <code>()</code>, <code>[]</code>, <code>{}</code>, and quotes balanced?</li><li>Does every custom variable start with <code>#</code>?</li><li>Does <code>#this</code> refer to the intended nesting level in each selection or projection?</li><li>Are possibly empty selections, map values, <code>returnObj</code>, and <code>throwExp</code> protected by a branch?</li><li>Was a command option incorrectly placed inside the expression, or a complete command placed in a tool&#39;s expression field?</li><li>Did automatic formatting split a class name, method name, or operator?</li></ul><p>This process\u2014confirm context, execute incrementally, then construct the final output\u2014is more reliable than rewriting an entire complex expression after every failure, and it preserves evidence about the actual cause.</p><h2 id="further-reading" tabindex="-1"><a class="header-anchor" href="#further-reading" aria-hidden="true">#</a> Further reading</h2>`,10),K={href:"https://github.com/alibaba/arthas/issues/71",target:"_blank",rel:"noopener noreferrer"},$=t("Arthas issue #71: special OGNL usages"),z={href:"https://commons.apache.org/dormant/commons-ognl/language-guide.html",target:"_blank",rel:"noopener noreferrer"},Q=t("Apache Commons OGNL Language Guide");function Y(X,Z){const s=r("RouterLink"),i=r("ExternalLinkIcon");return l(),c("div",null,[h,p,e("p",null,[m,a(s,{to:"/en/doc/ognl.html"},{default:n(()=>[v,b]),_:1}),g,a(s,{to:"/en/doc/advice-class.html"},{default:n(()=>[x]),_:1}),f,y,_]),k,e("table",null,[q,e("tbody",null,[w,j,e("tr",null,[L,e("td",null,[a(s,{to:"/en/doc/advice-class.html"},{default:n(()=>[A]),_:1}),S,N,C,T,O,M,E,I,D,G]),R]),V,J,P])]),F,e("p",null,[H,a(s,{to:"/en/doc/batch-support.html"},{default:n(()=>[U]),_:1}),B]),W,e("ul",null,[e("li",null,[e("a",K,[$,a(i)])]),e("li",null,[e("a",z,[Q,a(i)])])])])}const te=d(u,[["render",Y],["__file","ognl-express.html.vue"]]);export{te as default};
